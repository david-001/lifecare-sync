import React from "react";

const Home = () => {
  return (
    <div className="homeContainerMiddleBody">
      <div className="hold-afterpicture">
        <h2 className="h2lifecaresync">Lifecare Sync</h2>

        <h3>See the Patients</h3>
        {/* {user ? (
          <PatientIndex msgAlert={msgAlert} user={user} />
        ) : (
          <p>log in to see patients.</p>
        )} */}
        <p>Login to see patients.</p>
      </div>
    </div>
  );
};

export default Home;
